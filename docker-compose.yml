version: '3.8'

services:
  name_scandi_bot:
    build: .
    environment:
      - TOKEN_TG=${TOKEN_TG}
      - OPENAI_KEY=${OPENAI_KEY}
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
